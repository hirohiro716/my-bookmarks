version: '3'

services:
    php:
        image: php:7.4-apache
        volumes: 
            - ./docker:/var/www/html
        ports:
            - "8080:80"
        container_name: my-bookmarks
        user: "www-data:www-data"
